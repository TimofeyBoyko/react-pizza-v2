import styled from "styled-components";

const StyledSizesSelector = styled.div`
  width: 100%;
  height: 32px;

  display: grid;
  align-items: center;
  grid-template-columns: 1fr 1fr 1fr;

  grid-gap: 0 10px;
`;

export default StyledSizesSelector;
